class NthPrime {
  static final _primeList = <int>[];
  int prime(int nth) {
    if (nth <= _primeList.length) {
      return _primeList[nth - 1];
    } else {
      int currentNum = 2
      while (_primeList.length < nth) {

      }
    }
  }
}
